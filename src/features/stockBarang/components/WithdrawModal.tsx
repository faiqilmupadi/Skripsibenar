"use client";
import { Modal } from "@/components/ui/Modal";
export function WithdrawModal({ open }: { open: boolean }) { return <Modal open={open} title="WithdrawModal">WithdrawModal form (stub)</Modal>; }
