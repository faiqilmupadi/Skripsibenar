"use client";
import { Modal } from "@/components/ui/Modal";
export function ReturnModal({ open }: { open: boolean }) { return <Modal open={open} title="ReturnModal">ReturnModal form (stub)</Modal>; }
