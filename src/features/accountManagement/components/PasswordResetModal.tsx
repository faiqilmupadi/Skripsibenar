"use client";
import { Modal } from "@/components/ui/Modal";
export function PasswordResetModal({ open }: { open: boolean }) { return <Modal open={open} title="Reset Password">Form reset password (stub)</Modal>; }
