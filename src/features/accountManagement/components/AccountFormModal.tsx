"use client";
import { Modal } from "@/components/ui/Modal";
export function AccountFormModal({ open }: { open: boolean }) { return <Modal open={open} title="Admin Baru">Form tambah admin (stub)</Modal>; }
