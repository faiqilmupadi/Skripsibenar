export type AdminPerformance = { name: string; total: number; percent: number };
export type ItemFsn = { name: string; ratio: number; status: string; percent: number };
export type AssetPoint = { date: string; value: number };
